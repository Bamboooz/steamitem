import React, { useEffect } from "react";

const Item: React.FC = () => {
    const itemUrl = "https://steamcommunity.com/market/listings/730/Kilowatt%20Case";

    const fetchItemData = () => {

    };

    useEffect(() => {
        
    }, []);

    return (
        <>
            
        </>
    );
};

export default Item;
//validate item url regex, if failed, error
//send request for data
//if failed, error else open the item page and give it the data
// analyze it from here